{
  "name": "darkstr",
  "version": "0.1.2-phase1",
  "private": true,
  "description": "darkstr Phase 1 — Firefox WebExtension. Pollution tool, not anti-detect / not Cloudflare bypass. Not official LibreWolf.",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node esbuild.config.mjs",
    "build:check": "node esbuild.config.mjs && git diff --exit-code extension/anti-fingerprint-bootstrap.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alex-hinojosa/darkstr.git"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
