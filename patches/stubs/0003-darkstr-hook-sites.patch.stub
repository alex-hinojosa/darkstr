# STUB POINTER — real unified diff is:
#   patches/0003-darkstr-native-persona-hooks.patch
#
# M3 live (2026-09-14): train-pinned chrome JS on LibreWolf/Firefox 155.0.1-1.
#   DarkstrNativePersona.sys.mjs + Parent/Child JSWindowActor
#   Wired via BrowserGlue + moz.build (requires M2 DarkstrModeXor context)
#
# Surfaces landed in real patch (not C++):
#   nsHttp via http-on-modify-request — UA override + CH REMOVE (never SET)
#   Navigator minimum fields via JSWindowActor child
#   DocShell-ish first vs subsequent via top-level document load count
#   Feature flag darkstr.nativePersonaHooks (+ WebExt MAIN inject gate)
#
# Glue reads cached snapshot only when pollution_active:
#   prefs darkstr.persona.snapshot (JSON) or darkstr.persona.seed fallback
# Homogeneous / Native-Compatible → idle
#
# Authoritative enums (Rust control plane on main):
#   duppel_bridge::HookSite / HookApplicatorSurface / NsHttpAction / NavigatorField
#   duppel_persona::DocShellNavPhase / NativePersonaPlan / ClientHintsPolicy::Remove
#
# Apply: after 0002, patch -d "$DARKSTR_GECKO_ROOT" -p1 < patches/0003-darkstr-native-persona-hooks.patch
# Atlas: never mv under /Volumes/Mesh.
#
# Brand: darkstr — not official LibreWolf. No Cloudflare/TLS claims.
# Status: docs/M3-STATUS.md · Hooks: docs/GECKO-HOOKS.md §2
