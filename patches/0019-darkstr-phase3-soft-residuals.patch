# darkstr Phase 3 soft residuals — train-pinned follow-up (0019) on 155.0.1-1
# Train: Firefox FIREFOX_155_0_1_RELEASE / LibreWolf 155.0.1-1
# Brand: darkstr — not official LibreWolf. No Cloudflare/TLS/JA3 claims.
# Apply (after 0018): patch -d "$DARKSTR_GECKO_ROOT" -p1 < patches/0019-darkstr-phase3-soft-residuals.patch
# Requires Phase 3 pins 1–3 (DepthHooks + WorkerHooks) already on the tree.
# Fixes: worker lastInstall overwrite; OfflineAudio getChannelData fill/deltaSum;
#        best-effort worker hardwareConcurrency (honest residual if non-configurable).
# Default-off unchanged. Lessons baked: waiveXrays, pageshow, getPrefType, safeForUntrusted, install-dist_bin.
#
diff -ruN a/browser/components/DarkstrDepthHooksChild.sys.mjs b/browser/components/DarkstrDepthHooksChild.sys.mjs
--- a/browser/components/DarkstrDepthHooksChild.sys.mjs	2026-09-20 18:12:28.764977680 +0000
+++ b/browser/components/DarkstrDepthHooksChild.sys.mjs	2026-09-20 18:12:28.768977677 +0000
@@ -275,40 +275,131 @@
     }
 
     // AudioContext and OfflineAudioContext both expose AudioBuffer.
+    // Soft residual (pin 2): OfflineAudio fill patterns getChannelData → write →
+    // read again. A plain once-per-channel Set skipped re-noise after fill
+    // (deltaSum 0) while canvas mutation still PASS. Latch on a cheap content
+    // key so fill()/set() invalidates; mutate via Float32Array.set so Xray
+    // OfflineAudio buffers keep the writes. Also noise startRendering results.
     if (pageWindow.AudioBuffer?.prototype?.getChannelData) {
       const audioProto = pageWindow.AudioBuffer.prototype;
       const origGetChannelData = Object.getOwnPropertyDescriptor(
         audioProto,
         "getChannelData"
       )?.value;
+      // buffer -> Map(channel -> contentKey after last successful noise)
       const noisedBuffers = new WeakMap();
+
+      function channelContentKey(rawData) {
+        try {
+          const n = rawData.length | 0;
+          if (!n) {
+            return "0";
+          }
+          return `${n}:${rawData[0]}:${rawData[n >> 1]}:${rawData[n - 1]}`;
+        } catch (_e) {
+          return "x";
+        }
+      }
+
+      function applyAudioNoise(rawData, channel) {
+        const seed = audioSeed ^ (channel * 0x9e3779b9);
+        const copy = new Float32Array(rawData.length);
+        for (let i = 0; i < rawData.length; i++) {
+          let h = seed ^ (i * 2654435761);
+          const sample = rawData[i];
+          h = Math.imul(h ^ ((sample * 1e6) >>> 0), 0x45d9f3b);
+          h = (h ^ (h >>> 16)) >>> 0;
+          copy[i] = sample + ((h % 200) - 100) * 0.0000005;
+        }
+        rawData.set(copy);
+      }
+
+      function markNoised(audioBuffer, channel, rawData) {
+        let map = noisedBuffers.get(audioBuffer);
+        if (!map) {
+          map = new Map();
+          noisedBuffers.set(audioBuffer, map);
+        }
+        map.set(channel, channelContentKey(rawData));
+      }
+
+      function noiseAudioBufferChannels(audioBuffer) {
+        try {
+          const n = (Cu.waiveXrays(audioBuffer).numberOfChannels | 0);
+          for (let ch = 0; ch < n; ch++) {
+            const data = Reflect.apply(origGetChannelData, audioBuffer, [ch]);
+            const rawData = Cu.waiveXrays(data);
+            applyAudioNoise(rawData, ch);
+            markNoised(audioBuffer, ch, rawData);
+          }
+        } catch (error) {
+          onRuntimeError("audio.buffer", error);
+        }
+      }
+
       replaceMethod(
         pageWindow,
         audioProto,
         "getChannelData",
         function (channel) {
           const data = Reflect.apply(origGetChannelData, this, [channel]);
-          let noised = noisedBuffers.get(this);
-          if (!noised) {
-            noised = new Set();
-            noisedBuffers.set(this, noised);
-          }
-          if (noised.has(channel)) {
-            return data;
-          }
-          noised.add(channel);
-          const rawData = Cu.waiveXrays(data);
-          const seed = audioSeed ^ (channel * 0x9e3779b9);
-          for (let i = 0; i < rawData.length; i++) {
-            let h = seed ^ (i * 2654435761);
-            h = Math.imul(h ^ ((rawData[i] * 1e6) >>> 0), 0x45d9f3b);
-            h = (h ^ (h >>> 16)) >>> 0;
-            rawData[i] += ((h % 200) - 100) * 0.0000005;
+          try {
+            const rawData = Cu.waiveXrays(data);
+            let map = noisedBuffers.get(this);
+            if (!map) {
+              map = new Map();
+              noisedBuffers.set(this, map);
+            }
+            const key = channelContentKey(rawData);
+            if (map.get(channel) === key) {
+              return data;
+            }
+            applyAudioNoise(rawData, channel);
+            map.set(channel, channelContentKey(rawData));
+          } catch (error) {
+            onRuntimeError("audio.getChannelData", error);
           }
           return data;
         },
         replacements
       );
+
+      const OfflineAudioContext = pageWindow.OfflineAudioContext;
+      if (OfflineAudioContext?.prototype) {
+        const oacProto = OfflineAudioContext.prototype;
+        const origStartRendering = Object.getOwnPropertyDescriptor(
+          oacProto,
+          "startRendering"
+        )?.value;
+        if (typeof origStartRendering === "function") {
+          replaceMethod(
+            pageWindow,
+            oacProto,
+            "startRendering",
+            function (...args) {
+              const result = Reflect.apply(origStartRendering, this, args);
+              try {
+                const waived = Cu.waiveXrays(result);
+                if (waived && typeof waived.then === "function") {
+                  return waived.then(
+                    Cu.exportFunction(function (buffer) {
+                      noiseAudioBufferChannels(buffer);
+                      return buffer;
+                    }, pageWindow)
+                  );
+                }
+                if (result) {
+                  noiseAudioBufferChannels(result);
+                }
+              } catch (error) {
+                onRuntimeError("audio.startRendering", error);
+              }
+              return result;
+            },
+            replacements
+          );
+        }
+      }
     }
 
     installedByWindow.set(rawWindow, { replacements });
diff -ruN a/browser/components/DarkstrWorkerHooks.sys.mjs b/browser/components/DarkstrWorkerHooks.sys.mjs
--- a/browser/components/DarkstrWorkerHooks.sys.mjs	2026-09-20 18:12:28.772977674 +0000
+++ b/browser/components/DarkstrWorkerHooks.sys.mjs	2026-09-20 18:12:28.772977674 +0000
@@ -317,8 +317,13 @@
     return this._plan || this.refreshPlan();
   },
 
-  /** Persist content-install diagnostics. Never writes privacy.*. */
+  /**
+   * Persist content-install diagnostics. Never writes privacy.*.
+   * Soft residual: runtime construct fallbacks set runtimeOnly and must not
+   * overwrite lastInstall after a successful wrap (ok stays installed).
+   */
   recordInstallStatus(detail, browsingContext = null) {
+    const runtimeOnly = !!detail?.runtimeOnly;
     const payload = {
       ok: !!detail?.ok,
       event: String(detail?.event || "unknown"),
@@ -331,6 +336,17 @@
         payload.bc = browsingContext.id;
       }
     } catch (_e) {}
+    if (runtimeOnly) {
+      try {
+        Services.prefs.setStringPref(
+          LAST_ERROR_PREF,
+          `runtime:${payload.status}:${payload.error}`.slice(0, 1000)
+        );
+      } catch (e) {
+        console.error("darkstr worker lastError write failed", e);
+      }
+      return;
+    }
     try {
       Services.prefs.setStringPref(LAST_INSTALL_PREF, JSON.stringify(payload));
     } catch (e) {
diff -ruN a/browser/components/DarkstrWorkerHooksChild.sys.mjs b/browser/components/DarkstrWorkerHooksChild.sys.mjs
--- a/browser/components/DarkstrWorkerHooksChild.sys.mjs	2026-09-20 18:12:28.768977677 +0000
+++ b/browser/components/DarkstrWorkerHooksChild.sys.mjs	2026-09-20 18:12:28.768977677 +0000
@@ -76,18 +76,37 @@
     ? persona.languages
     : ["en-US", "en"];
   const appVersion = String(persona.userAgent || "").replace(/^Mozilla\//, "");
+  const hw = Number(persona.hardwareConcurrency) || 8;
+  const mem = Number(persona.deviceMemory) || 8;
+  // Prefer own-property overrides on the navigator instance. WorkerNavigator
+  // prototype hardwareConcurrency is often non-configurable from C++; proto
+  // defineProperty then silently fails and Proof still sees host cores.
   return `
 (function(){
   try {
-    var proto = self.navigator && self.navigator.__proto__;
-    if (!proto) { return; }
-    Object.defineProperty(proto, "userAgent", { configurable: true, enumerable: true, get: function(){ return ${JSON.stringify(persona.userAgent)}; } });
-    Object.defineProperty(proto, "platform", { configurable: true, enumerable: true, get: function(){ return ${JSON.stringify(persona.platform || "MacIntel")}; } });
-    Object.defineProperty(proto, "hardwareConcurrency", { configurable: true, enumerable: true, get: function(){ return ${Number(persona.hardwareConcurrency) || 8}; } });
-    Object.defineProperty(proto, "deviceMemory", { configurable: true, enumerable: true, get: function(){ return ${Number(persona.deviceMemory) || 8}; } });
-    Object.defineProperty(proto, "language", { configurable: true, enumerable: true, get: function(){ return ${JSON.stringify(langs[0] || "en-US")}; } });
-    Object.defineProperty(proto, "languages", { configurable: true, enumerable: true, get: function(){ return Object.freeze(${JSON.stringify(langs)}); } });
-    Object.defineProperty(proto, "appVersion", { configurable: true, enumerable: true, get: function(){ return ${JSON.stringify(appVersion)}; } });
+    var nav = self.navigator;
+    if (!nav) { return; }
+    var proto = nav.__proto__;
+    function def(obj, prop, getter) {
+      if (!obj) { return false; }
+      try {
+        Object.defineProperty(obj, prop, { configurable: true, enumerable: true, get: getter });
+        return true;
+      } catch (_e) { return false; }
+    }
+    function spoof(prop, getter) {
+      if (def(nav, prop, getter)) { return true; }
+      return def(proto, prop, getter);
+    }
+    spoof("userAgent", function(){ return ${JSON.stringify(persona.userAgent)}; });
+    spoof("platform", function(){ return ${JSON.stringify(persona.platform || "MacIntel")}; });
+    // Best-effort: instance first, then proto. If both refuse (non-configurable
+    // host binding), hardwareConcurrency residual stays host — documented.
+    spoof("hardwareConcurrency", function(){ return ${hw}; });
+    spoof("deviceMemory", function(){ return ${mem}; });
+    spoof("language", function(){ return ${JSON.stringify(langs[0] || "en-US")}; });
+    spoof("languages", function(){ return Object.freeze(${JSON.stringify(langs)}); });
+    spoof("appVersion", function(){ return ${JSON.stringify(appVersion)}; });
   } catch (_e) {}
 })();`;
 }
@@ -318,6 +337,27 @@
     }
   }
 
+  /**
+   * Construct-time fallback (blob wrap threw → OrigWorker). Must NOT clobber
+   * lastInstall after a successful constructor wrap — Proof soft residual:
+   * lastInstall often ended ok:false/runtime-Worker even when persona UA landed.
+   * Runtime surfaces go to lastError only (runtimeOnly).
+   */
+  _reportRuntime(surface, error) {
+    const detail = {
+      ok: false,
+      event: "runtime",
+      status: `runtime-${surface}`,
+      error: error ? errorText(error).slice(0, 1000) : "",
+      runtimeOnly: true,
+    };
+    try {
+      this.sendAsyncMessage("DarkstrWorkerHooks:InstallStatus", detail);
+    } catch (reportError) {
+      console.error("darkstr worker runtime diagnostic IPC failed", reportError, detail);
+    }
+  }
+
   async pullAndInstall(eventType) {
     let payload;
     try {
@@ -348,7 +388,7 @@
     try {
       const status = installWorkerHooks(rawWindow, payload, (surface, error) => {
         console.error(`darkstr worker runtime failure (${surface})`, error);
-        this._report(false, eventType, `runtime-${surface}`, error);
+        this._reportRuntime(surface, error);
       });
       this._report(true, eventType, status);
     } catch (error) {
