[package]
name = "duppel-ffi"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Reviewed Gecko FFI boundary for darkstr — only crate that may use unsafe"
publish = false

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
duppel-persona = { path = "../duppel-persona" }
