[package]
name = "duppel-coherence"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "darkstr / Duppel HTTP↔JS coherence harness for Proof QA (Phase 2 control-plane API)"
publish = false

[dependencies]
duppel-persona = { path = "../duppel-persona" }

[lib]
path = "src/lib.rs"
